# Emotion-Detector

Using the RAVDESS dataset. after processing the wav audio files into mel spctrograms and passing it into the network. The Saveee data set was used for testing . The proposed network is one of CNNs and Max pooling layers followed by fully connected layers
